<UserControl x:Class="UTAU.Views.LyricsBulkEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:root="clr-namespace:UTAU"
             xmlns:vm="clr-namespace:UTAU.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="560"
             d:DataContext="{d:DesignInstance Type={x:Type vm:LyricsBulkEditViewModel}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="26"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="LyricBox" Grid.Row="0"
                 Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 SpellCheck.IsEnabled="False"
                 ToolTip="{x:Static root:Texts.LyricsBulkEditHint}"/>

        <Grid Grid.Row="1">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Text="{Binding CountText}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Opacity" Value="0.75"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasMismatch}" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="88" Height="26" Content="{x:Static root:Texts.ButtonReset}" Command="{Binding ResetCommand}"/>
                <Button Width="88" Height="26" Content="{x:Static root:Texts.ButtonCancel}" IsCancel="True"/>
                <Button x:Name="ApplyButton" Width="88" Height="26" IsDefault="True"
                        Content="{x:Static root:Texts.ButtonApply}" Click="Apply_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
